# Muse — top-level Python dependencies (Python 3.12)
# Portable pinned set. Regenerated 2026-04-21 from the prior conda-frozen file,
# with absolute wheel paths removed and mediapipe added (missing from the freeze).
#
# Note: EyeBall/yolo_detection.py depends on a local PyTorch-YOLOv3 checkout
# (torch, torchvision, Pillow, matplotlib). Install that separately — see
# EyeBall/yolo_detection.md.

imutils==0.5.4
mediapipe~=0.10
numpy==2.2.2
opencv-python==4.10.0
pandas==2.2.3
scipy==1.15.1
shapely~=2.0
